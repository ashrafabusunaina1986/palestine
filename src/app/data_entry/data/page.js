
import React from 'react'
import fs from 'fs'
import data from '@/app/data_entry/data.json'



function page() {
  // data.push(info)
  // fs.promises.writeFile('@/app/data_entry/data.json',JSON.stringify(data),'utf-8')
  // .then(res=>{}).catch(err=>{console.log(err)})
  return (
    <div></div>
  )
}

export default page